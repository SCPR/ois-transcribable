<h1>Welcome <%= @user.email %></h1>

<p>This is the first page someone will see. It should have a link to create a <%= link_to 'new transcription', controller: :transcriptions, action: :new %>.</p>

<p>It should probably also display some dashboard data for each user?</p>

<% @incidents = Incident.all %>

<p>For instance, there are <%= @incidents.size %> documents in the database</p>

<!--
<ul>
    <%# @incidents.each do |incident| %>
        <li><strong><%#= incident.url %></strong></li>
    <%# end %>
</ul>
-->

<ul>
    <% @users = User.all %>
    <% @users.each do |user| %>
        <li><strong><%= user.email %></strong>: (Transcribed so far)</li>
    <% end %>
</ul>

<%= render 'devise/menu/login_items' %>
